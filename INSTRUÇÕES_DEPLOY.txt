## 🚀 COMO FAZER O DEPLOY AGORA

### 1️⃣ FAZER PUSH PARA GITHUB

```bash
# Se ainda não configurou o repositório remoto:
git remote add origin https://github.com/SEU_USUARIO/SEU_REPO.git

# Fazer push:
git push -u origin master

# Para próximos pushes:
git push
```

### 2️⃣ CLONAR NA HOSTGATOR VIA SSH

```bash
# Conectar na HostGator:
ssh vin31871@viniciusbaggio1772594984234.linuxpro.com.br

# Depois de conectado:
cd ~
rm -rf public_html/*

# Clone (use um token do GitHub para melhor segurança):
git clone https://github.com/SEU_USUARIO/SEU_REPO.git public_html

# Ou use token para repositório privado:
# git clone https://SEU_TOKEN@github.com/SEU_USUARIO/SEU_REPO.git public_html

# Configure permissões:
cd public_html
chmod 755 logs assets
chmod 644 .env .htaccess index.php
```

### 3️⃣ VERIFICAR NO NAVEGADOR

```
https://viniciusbaggio1772594984234.0452201.meusitehostgator.com.br/
```

---

## ✅ O QUE ESTÁ PRONTO

- ✅ Projeto com `.env` configurado
- ✅ `.htaccess` com regras básicas
- ✅ Tudo commitado no Git (sem .gitignore)
- ✅ README com instruções completas
- ✅ Banco de dados em `assets/ripfire_db.sql`
- ✅ DEBUG ativado (para ver erros)
- ✅ APP_URL apontando para HostGator

---

## 📝 ARQUIVOS COMMITADOS

```
.env                    ← Todas as credenciais aqui!
.htaccess              ← Regras do Apache
.gitignore             ← Vazio agora
README.md              ← Instruções de deploy
DEPLOY_SSH.sh          ← Script de referência
index.php              ← Ponto de entrada
src/                   ← Todos os controllers/models/views
assets/                ← CSS, uploads, e SQL do banco
manifest.json          ← PWA
sw.js                  ← Service Worker
```

---

## 🔧 CREDENCIAIS JÁ CONFIGURADAS

```
DB_HOST=localhost
DB_NAME=vin31871_ripfire
DB_USER=vin31871_ripfire
DB_PASSWORD=Vb357753@
APP_DEBUG=true         ← Mostra erros em localhost
APP_URL=https://viniciusbaggio1772594984234.0452201.meusitehostgator.com.br/
```

---

**PRÓXIMO PASSO:** Fazer o `git push` para GitHub e depois clonar na HostGator! 🚀